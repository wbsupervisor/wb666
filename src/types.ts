export interface GameCategory {
  title: string;
  description: string;
  image: string;
  link: string;
}

export interface Testimonial {
  title: string;
  content: string;
  stars: number;
}

export interface FAQItem {
  question: string;
  answer: string;
}
